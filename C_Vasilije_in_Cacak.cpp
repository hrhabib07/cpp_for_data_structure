#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        long long int n, k, x;
        cin >> n >> k >> x;
        long long int sum = (n * (n + 1)) / 2;
        cout << sum << endl;
        // if (k < x && sum >= x)
        // {

        //     cout << "YES\n";
        // }
        // else
        // {
        //     cout << "NO\n";
        // }
    }

    return 0;
}